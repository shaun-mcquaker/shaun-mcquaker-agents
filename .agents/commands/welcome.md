Print the following exactly to the user.

# Welcome

This repo is Shaun McQuaker's local tooling and AI workflow config.
It keeps the OpenCode setup, repo-local agent content, plugins, shell config, Neovim config, Zellij config, Ghostty config, and helper scripts in one versioned place.
Run `qstack install` to wire the checked-in files into `~/.config` and `~/.local/bin` so the live environment matches the repo.

       /\
      /  \
     /____\\
     |    |
     | [] |
     |    |
     |    |
    /| |  |\\
   /_|_|__|_\\
      /_/\_\\
